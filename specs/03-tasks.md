# 03 · Tareas

Cada tarea traza a requisitos (RF/RNF) y a la sección de la guía (`docs/guia-lanzamiento.md`) que explica el cómo. **Responsable:** 👤 solo humano (credenciales) · 🤝 humano siguiendo la guía, con apoyo del agente. **Estado:** `[x]` hecha · `[~]` parcial · `[ ]` pendiente.

## Fase 0 · Lanzamiento (hoy)

- [x] **T-001** 👤 Crear el despliegue en ushahidi.io — *Hecho 10-ago-2026:* https://sos-sismo-colombia.ushahidi.io/ · (RF-12 · Guía §1)
- [x] **T-002** 🤝 Ajustes generales · (RF-02, RNF-02, RNF-03 · Guía §2) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`*: nombre, es, America/Bogota, mapa 5.0/-76.2 z7, descripción con aviso, publicación instantánea ✅, autor oculto ✅
- [x] **T-003** 🤝 Crear las 10 categorías · (RF-01 · Guía §3) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`* · sin emoji (ADR-007)
- [x] **T-004** 🤝 Encuesta «Pido ayuda» · (RF-01…RF-04, RF-06 · Guía §4) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`*: 16 campos (14 + Título y Descripción nativos), 2 protegidos ✅
- [x] **T-005** 🤝 Encuesta «Busco a un familiar» · (RF-07 · Guía §5) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`*: 9 campos, 1 protegido ✅
- [x] **T-006** 🤝 Encuesta «Quiero ayudar» · (RF-08 · Guía §6) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`*: 8 campos, 6 protegidos ✅ · Título/Descripción nativos públicos por diseño (ADR-006)
- [x] **T-007** 🤝 Rol «Ayudante verificado» · (RF-03, RF-08 · Guía §7) — *Hecho 10-ago-2026 vía `scripts/aplicar_config.py`* con permiso «Manage Posts». **Asignarlo a personas sigue siendo manual tras la llamada (ADR-005).**
- [ ] **T-008** 🤝 Búsquedas guardadas de verificación («🔴 Críticas sin verificar», «Pendientes por municipio», «Resueltas hoy») · (RF-05 · Guía §8)
- [~] **T-009** 👤 SMS: SIM local ✅ (11-ago-2026) · activar SMSSync en Fuentes de datos y definir la clave, montar la pasarela SMS→HTTP en el Android, respuesta automática, **probar con un SMS real** y volver a auditar · (RF-09, ADR-018 · Guía §9) — **La app SMSsync queda descartada:** `targetSdkVersion 22` (feb-2017) y Android 14+ se niega a instalarla; el teléfono del equipo es Android 14/15 y no hay otro. Se sustituye por un automatizador moderno que hable el mismo protocolo. Endpoint ya sondeado y vivo: `POST /sms/smssync` → 403 con secreto inválido. **Estado al 11-ago-2026 (leído por API):** la fuente `smssync` está `enabled: true` pero con `params: []`, es decir **sin secreto**. Y `verifySecret()` de `SMSSync.php` exige que el secreto exista *y* coincida, así que hoy el canal rechaza todo con 403 — falla cerrado, que es lo correcto, pero está inutilizable. **Siguiente paso:** definir el secreto en Configuración → Fuentes de datos → SMSSync (campo obligatorio) y usar el mismo en la pasarela del teléfono.
- [~] **T-010** 🤝 Prueba de punta a punta · (RF-02, RF-05, RF-06 · Guía §14) — *Ciclo verificado por API el 10-ago-2026:* ensayo con datos ficticios creado → publicado al instante → verificado → «En atención» → «Resuelta» → archivado (deja de ser visible; 0 publicaciones públicas). **Comprobado además que un anónimo NO recibe dirección ni teléfono ✅.** Falta la pasada humana por el navegador (llenar el formulario como lo haría un afectado).
- [ ] **T-011** 👤 Probar exportación CSV y definir las dos ubicaciones de respaldo (fuera del repo) · (RF-11, P2 · Guía §10) — **los dos destinos DEBEN estar cifrados y con caducidad declarada** (ADR-024): cada respaldo es una copia completa, con teléfonos y direcciones exactas, **fuera** de Ushahidi. Es hoy la mayor superficie de fuga del sistema y, al contrario que todo lo demás de esta lista, está enteramente en nuestras manos.
- [~] **T-012** 🤝 Número SMS real **3148071191** en todo el repositorio · (RF-09 · Guía §12) — *Hecho 11-ago-2026:* YAML (descripción del sitio, `sms.numero`, `sent_to`, texto de WhatsApp), guía y `docs/senales-que-salvan.md`. El enlace difundido pasa al dominio propio **https://www.sossismocolombia.com.co/** (`despliegue.url_publica`; `despliegue.url` sigue siendo el backend porque de ahí sale `base_api()` en los tres scripts). La descripción del despliegue ya lleva el número (`aplicar_config.py --aplicar`, 11-ago-2026, 1 cambio; auditoría pública en verde). **Falta un paso humano:** `NEXT_PUBLIC_SMS=3148071191` está puesta en Vercel pero **la web en vivo aún no la muestra** — es una variable `NEXT_PUBLIC_`, se incrusta al compilar, así que hay que redesplegar.
- [ ] **T-013** 👤 Difusión: enviar el texto de WhatsApp a PMU (Bogotá, Cali, Armenia, Quibdó), UNGRD, Cruz Roja, Defensa Civil, Bomberos, alcaldías afectadas y emisoras locales (que lean el número SMS al aire) · (RF-10, P1 · Guía §12)

- [x] **T-014** 🤝 Cara pública propia (`web/`, Next.js) con los 3 formularios, mapa Leaflet + Humanitarian OSM y aviso de privacidad · (RF-01…RF-08, ADR-010, ADR-011) — *Hecho 10-ago-2026:* lee el esquema de la API en vivo, publica anónimo (201), oculta «Estado»/«Verificación» y los envía por código. Falta subir a Vercel (T-015).
- [x] **T-015** 👤 Desplegar `web/` en Vercel y decidir qué enlace se difunde · (P1) — *Hecho 11-ago-2026:* al aire en **https://www.sossismocolombia.com.co/** (dominio propio, HTTP 200 verificado) y ese es el enlace que va en toda la difusión. `plataforma-ayuda-sismo-colombia.vercel.app` sigue respondiendo pero no se difunde.
- [ ] **T-016** 🤝 Subida de fotos en el front propio (requiere multipart a `/api/v5/media`) · (RF-01)
- [x] **T-017** 🤝 Aplicar el parche del 11-ago: `python scripts/aplicar_config.py --aplicar` para crear la categoría «Iluminación / energía», la encuesta «Ofrezco recursos» y la descripción con la línea 123 · (RF-14, RNF-06, ADR-015) — *Hecho 11-ago-2026:* 3 cambios aplicados (descripción del sitio, categoría id 11, encuesta de 9 campos con 2 protegidos) y auditoría de privacidad en verde. **Pendiente T-023:** la categoría nueva no entró en «¿Qué necesitas?» de «Pido ayuda».
- [x] **T-018** 🤝 Front: banner de la línea 123 **arriba** en la portada y formulario `/ofrezco-recursos` ocultando «Estado del recurso» según ADR-011 · (RF-14, RF-15, RNF-06) — *Hecho 11-ago-2026:* el banner vive en el layout, así que abre **todas** las páginas y no solo la portada (RNF-06 pide «toda cara pública»), con el 123 como enlace `tel:`. `/ofrezco-recursos` muestra arriba la regla de la maquinaria y oculta «Estado del recurso». Verificado en navegador real a 360 px y 1280 px.
- [~] **T-023** 🤝 ~~Añadir la categoría «Iluminación / energía» a «¿Qué necesitas?»~~ · **Descartada el 11-ago-2026 por decisión del equipo:** hace falta luz en toda la zona, así que la marcaría casi todo el mundo y saturaría el triaje en vez de ordenarlo. Retirada del YAML; en el despliegue quedó creada y sin usar (id 11) — borrarla es manual y no corre prisa. Sigue disponible como opción de «Qué ofreces».
- [x] **T-024** 🤝 Ofertas fuera del ojo público y mapa legible · (RF-16, ADR-016) — *Hecho 11-ago-2026:* «Quiero ayudar» y «Ofrezco recursos» pasan a aprobación previa (solo las ve el equipo); el post 19 que ya estaba al aire se despublicó con `PATCH`. Verificado sin token: 0 ofertas visibles. El mapa y el listado filtran por encuesta, colorean búsqueda en azul aparte de la escala de urgencia, estrenan leyenda y desglosan el recuento. La confirmación de los dos formularios de oferta ya no promete publicación.
- [ ] **T-021** 👤 Difundir «Señales que salvan» (`docs/senales-que-salvan.md`) a emisoras y grupos: es el protocolo que funciona sin luz y sin internet · (P4, RF-10)
- [x] **T-022** 🤝 Arreglar los textos de `/quiero-ayudar` (RF-15): explicar el propósito arriba, cambiar «correo con el que creaste tu cuenta» por «correo — ahí te enviaremos el acceso cuando te verifiquemos», y cambiar el pie «se publica de inmediato» por «tus datos solo los ve el equipo; te llamaremos». Además, meta-descripción propia por página · (RF-15, P5) — *Hecho 11-ago-2026:* propósito y meta por página en `web/lib/ushahidi.ts`; el pie ya no promete publicación donde no la hay. La etiqueta del correo se corrige en el front (`TEXTOS`) porque renombrarla en la encuesta exigiría borrarla con los ayudantes ya registrados dentro; el YAML lleva la nota.
- [x] **T-020** 🤝 Validación de entrada y aviso de duplicados en el front · (RF-13, ADR-014) — *Hecho 10-ago-2026:* `web/lib/validacion.ts`. Probado en navegador real: punto en Europa bloquea; punto en Bogotá avisa; teléfono en campo público avisa; junto a dos ensayos en Quibdó salta el aviso de duplicado. Además `scripts/ensayos.py` siembra y retira solicitudes de prueba (solo borra las que empiezan por `[ENSAYO]`).
- [x] **T-025** 👤 Recuperar el acceso de administrador al despliegue · (T-009, RF-09) — *Hecho 11-ago-2026:* `.env` actualizado, `/oauth/token` responde 200 y `aplicar_config.py` vuelve a correr.
### Correcciones de formulario y puentes a lo oficial (11-ago-2026)

- [x] **T-033** 🤝 Municipio en **desplegable en cascada**, no texto libre · (RF-21, ADR-020) — *Hecho 11-ago-2026:* `web/lib/municipios.ts` con los 125 municipios (Chocó 30, Valle 42, Risaralda 14, Quindío 12, Caldas 27) y `SelectorMunicipio.tsx`. El backend no se toca: el campo sigue siendo `texto_corto` y recibe un string canónico. La vereda no se pierde — el valor queda `Pereira, Risaralda — vereda El Carmen` y agrupa por el prefijo. **Probado en navegador real** (`npm run probar`): el municipio arranca bloqueado, Risaralda trae sus 14, cambiar de departamento limpia el municipio anterior y la vereda sobrevive.
- [x] **T-034** 🤝 Sección opcional de acceso al final de `/pido-ayuda` · (RF-22, ADR-019) — *Hecho 11-ago-2026:* 5 campos nuevos (ids 79-83, todos públicos y opcionales) añadidos **sin borrar la encuesta** gracias a T-043; el front los agrupa en un `<details>` plegado bajo «Solo si tu zona es de difícil acceso». La ayuda de «Punto de referencia comunitario» empuja a la escuela o la cancha y avisa de que es público (P2).
- [x] **T-035** 🤝 Categoría «Vía bloqueada / acceso» en «¿Qué necesitas?» · (RF-22, ADR-019) — *Hecho 11-ago-2026:* creada y **metida en el campo**, que es donde T-023 se quedó a medias. «¿Qué necesitas?» pasa de 10 a 11 categorías, verificado contra la API.
- [x] **T-036** 🤝 Caducidad visible · (RF-23, ADR-021) — *Hecho 11-ago-2026:* `web/lib/frescura.ts`. Etiqueta «⏳ Sin confirmar hace N horas» en lista y globo del mapa pasadas 12 h, contrastada contra la **colección** de verificadas, nunca contra el campo falsificable. **El color de urgencia y el orden no se tocan** (ADR-021). Diez comprobaciones de la lógica en verde, incluidos los bordes: 11,9 h calla, 12,1 h avisa, una verificada nunca lleva etiqueta por vieja que sea, y un reloj adelantado no produce tiempos negativos.
- [x] **T-037** 🤝 Marca de frescura global · (RF-24) — *Hecho 11-ago-2026:* `Frescura.tsx`, calculada en el navegador para que una página cacheada no pueda afirmar una frescura que no tiene. Distingue «datos consultados hace X» de «última solicitud recibida hace Y». En la portada se busca la fecha sola tras pintar, para no meter una llamada de red en el camino crítico.
- [x] **T-038** 🤝 Bloque de líneas de emergencia en todas las páginas · (RF-25, RNF-06) — *Hecho 11-ago-2026:* en el **pie**, no arriba: arriba sigue el 123 y solo el 123, porque apilar siete números sobre el formulario lo echa fuera de la pantalla de un móvil. 123, 112, 132, 144, 119, 125 y 111, todos como enlaces `tel:`.
- [x] **T-039** 🤝 Búsqueda de familiares → canales oficiales · (RF-25) — *Hecho 11-ago-2026:* `/busco-familiar` **sigue activo** y se encabeza con el RCF de la Cruz Roja y el Registro Nacional de Desaparecidos. Se dice explícitamente que una cosa no reemplaza a la otra. *(El correo y el WhatsApp del RCF iban transcritos; salieron el 12-ago-2026 con T-048.)*
- [x] **T-040** 🤝 Vivienda dañada → SismoAyuda · (RF-25) — *Hecho 11-ago-2026:* salida desde `/pido-ayuda` hacia sismoayudaco.com/reportar, con la advertencia de que si hay alguien dentro se llama al 123. El formulario municipal de Itagüí queda enlazado en el centro de canales.
- [x] **T-041** 🤝 Donaciones → canales oficiales · (RF-25, ADR-022) — *Hecho 11-ago-2026:* Cruz Roja, ABACO, Fundación PLAN y los puntos de acopio de Bogotá, **enlazados y sin un solo número de cuenta transcrito**, encabezados por la advertencia de la Defensoría del Pueblo.
- [x] **T-042** 🤝 Fuentes oficiales de información · (RF-25) — *Hecho 11-ago-2026:* UNGRD, Servicio Geológico Colombiano, Invías (estado de las vías) y Defensoría, en `/enlaces-oficiales`.
- [x] **T-043** 🤝 **Reconciliación aditiva en `scripts/aplicar_config.py`** · (ADR-019) — *Hecho 11-ago-2026:* `anadir_campos()` relee la encuesta entera, añade lo que falta y hace `PUT` del objeto completo. **La trampa que había que resolver:** la API lee las categorías como objetos y las escribe como ids, así que reenviar el GET tal cual habría reventado — `_normalizar_opciones()` lo convierte. Comprobado con dos ensayos desechables (uno con campo `tags` y publicación dentro): ids de campos intactos, opciones unidas sin quitar ninguna, publicación viva con su valor. Idempotente: la segunda pasada aplica 0 cambios. Detalle en `docs/campos-en-encuesta-viva.md`.

### Privacidad y custodia de datos (12-ago-2026)

- [x] **T-044** 🤝 `/privacidad` declara dónde viven los datos · (RF-26) — *Hecho 12-ago-2026:* sección «Dónde viven tus datos» en `web/app/privacidad/page.tsx`, entre «Lo que solo ve el equipo» y «Lo que publicas no sale en Google», con la fecha de verificación en `ALOJAMIENTO_VERIFICADO` (ADR-022). **Decisión del equipo sobre la marcha:** ni nombres de proveedor ni cita de la circular de la SIC, y **el punto de reclamación es el correo, sin nombre propio** — la página dice «servidores en Irlanda», «la página llega desde Estados Unidos» y «leyes de protección de datos reconocidas en Colombia», y punto. Lo literal vive en ADR-024, que es donde lo busca quien lo necesita. `/privacidad` entra además en `npm run revisar`, donde no estaba pese a estar enlazada desde todas las páginas: verificada en navegador real a 360 px y 1280 px contra el build de producción, sin problemas.
  **Añadido al traer `main` (12-ago-2026):** la sección «Esto no sale en Google» decía que no se indexa *el sitio*, y con la indexación selectiva de **ADR-023** eso pasó a ser falso — solo `/mapa` queda fuera. Reescrita: se explica que el mapa es lo que no sale y que el resto sí, para no prometer una protección más ancha de la que hay justo en la página donde alguien viene a comprobarla. Mi ADR del alojamiento se renumeró de 023 a **024** por la colisión con el que `main` ya tenía mergeado.
- [x] **T-045** 🤝 Cerrar «Basic Post» y ampliar el auditor a **todas** las encuestas vivas · (RF-20, P2) — *Hecho 12-ago-2026.* (a) La encuesta de fábrica ya estaba en `disabled: true` y **eso no basta**: seguía saliendo en `/api/v5/surveys` para un anónimo y con `hide_author: false`, la única del despliegue que no ocultaba al autor. Ahora `aplicar_config.py` recorre `encuestas_de_fabrica` del YAML y le fuerza `disabled` + `hide_author` releyendo el objeto entero (ADR-019). (b) `auditar_publico.py` recorre lo que devuelve la API, no el YAML, y **falla ante cualquier encuesta viva no declarada**. **Lo que apareció al mirar:** dentro de «Basic Post» había 2 publicaciones, y una estaba **publicada, visible sin cuenta y dentro de la colección de verificadas** — era `[EJEMPLO] …`, dato de prueba que `scripts/ensayos.py` nunca limpió porque solo borra lo que empieza por `[ENSAYO]`. Pasada a borrador con `PATCH {"status":"draft"}` (ADR-016), sin borrar nada (RNF-04). Con eso el despliegue se quedó en **0 publicaciones visibles**, así que el auditor dejaba de poder probar dos cosas y las daba por buenas: ahora las declara en el veredicto («esta pasada NO ha comprobado…»), porque una auditoría que no dice lo que no miró miente igual que un mapa incompleto (RF-18).
- [x] **T-046** 🤝 Zona horaria del despliegue: estaba en **UTC**, no en `America/Bogota` · (RF-12) — *Hecho 12-ago-2026.* **La sospecha era falsa: el `PUT` NO la descarta.** Probado contra el despliegue vivo —leer, enviar, releer— y la guardó a la primera. No es una rareza de la plataforma como el `role` de ADR-006: la zona se torció por el camino y **nadie se enteró porque la auditoría se limitaba a imprimirla**. Ya está en `America/Bogota` y la auditoría ahora **falla** si `name`, `language`, `timezone` o `description` no coinciden con el YAML, o si el sitio se queda sin correo de contacto.
  **Dos cosas que salieron de debajo de la piedra:** (1) el `PUT` de `/config/site` **vacía lo que no viaja en el cuerpo** — el que manda `aplicar_config.py` borró el `email` del sitio, que es el canal público de borrado de RF-19, y respondió 200 tan tranquilo. Misma trampa que `default_view` (ADR-009, ampliada); ya se reenvía el objeto completo y el correo está repuesto. (2) El `# RNF-06` del YAML estaba **dentro** de un bloque `>`, donde no hay comentarios, así que era texto: la descripción pública del despliegue llevaba un « # RNF-06 » pegado al final. Corregido en el YAML y en el despliegue.
- [ ] **T-047** 👤 Pedir por escrito a `data@ushahidi.com` el **DPA**, la región de alojamiento confirmada y el plazo real de borrado · (RF-26) — sus términos dicen que los datos «pueden permanecer en copias de respaldo offline indefinidamente» y que el borrado no está garantizado; el resto de RF-26 se sostiene en una medición nuestra, no en un compromiso suyo (ADR-024).

- [x] **T-048** 🤝 Ningún contacto de organización transcrito: solo el enlace · (RF-25, ADR-022) — *Hecho 12-ago-2026 por decisión del equipo.* Salen del sitio el correo y el WhatsApp del RCF de la Cruz Roja, los dos celulares de la Defensa Civil del Chocó y la línea de la alcaldía de Itagüí; cada organización queda a un clic, en la página que ella mantiene. **Se quedan las siete líneas nacionales de tres dígitos** (123, 112, 132, 144, 119, 125, 111): no caducan y son lo único que sirve sin cargar nada (P4, RNF-06). El motivo es que la fecha de verificación no evita el daño —solo certifica que el dato era bueno el día que lo copiamos— y un celular de coordinación cambia de manos en días.

- [ ] **T-019** 👤 **Revisión del backend a las 72 h — 13-ago-2026** · (ADR-013) — decidir si seguimos con Ushahidi o migramos a Postgres propio. **Se migra si se cumple cualquiera:** (a) el equipo no usó el panel para triar; (b) el SMS quedó descartado; (c) 2+ rarezas nuevas bloquearon la operación. Si toca migrar: specs primero (P8). **ADR-024 no añade un cuarto criterio:** la privacidad no es motivo de migración —Irlanda y EE. UU. están en la lista de nivel adecuado de la SIC—, así que la revisión no se desvía por ahí. Lo que sí sale de ADR-024 es el disparador del **autoalojamiento** (red de más de ~10–15 ayudantes u operación de más de 3–4 semanas), que es una decisión distinta y posterior a esta.

## Fase 1 · Operación diaria (mientras dure la emergencia)

- [ ] **T-101** 👤 Ritual de verificación: Críticas → Altas → Medias, cada pocas horas · (RF-05 · Guía §8)
- [ ] **T-102** 👤 Llamar y dar de alta a cada ayudante que se registre; comunicar las 4 reglas; revocar ante incumplimiento · (RF-08 · Guía §7)
- [ ] **T-103** 👤 Estructurar los SMS entrantes: categoría, urgencia, pin · (RF-09 · Guía §9)
- [ ] **T-104** 👤 Exportar CSV mínimo 2 veces al día a las dos ubicaciones seguras · (RF-11)
- [ ] **T-105** 🤝 Anonimizar contacto de solicitudes Resueltas con más de 48 h · (P2)
- [x] **T-106** 🤝 Publicar este repositorio en GitHub (público) para réplica; confirmar que `.gitignore` excluye todo dato personal · (RF-12, P7) — *Hecho 10-ago-2026:* https://github.com/anduckia/plataforma-ayuda-sismo-colombia · auditoría sin datos personales ✅ · `.gitignore` reforzado ✅

## Fase 2 · Evolución (solo si la operación lo exige)

- [ ] **T-201** 🤝 Especificar en detalle RF-F2-01 (híbrido estricto) antes de escribir código · (P8)
- [ ] **T-202** 🤝 Despliegue autoalojado Docker + Cloudflare con importación de CSV · (RF-F2-02)
- [x] **T-203** 🤝 Automatizar la aplicación de `deployment-config.yml` vía API de Ushahidi · (RF-F2-03) — *Adelantada y hecha el 10-ago-2026 (P1):* `scripts/aplicar_config.py` idempotente, con simulacro por defecto y auditoría de privacidad que falla con código 1. Credenciales en `.env` local (nunca en el repo). Cubrió T-002…T-007.
- [ ] **T-204** 🤝 Script de anonimización para cierre de emergencia · (RF-F2-04)

## Regla de trabajo (P8)

Ninguna tarea nueva sin requisito; ningún requisito nuevo sin principio. Si durante la ejecución la interfaz real difiere de la guía, se corrige **primero** la guía o el YAML, y luego se marca la tarea.
